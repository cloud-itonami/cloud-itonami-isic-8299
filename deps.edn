{:paths ["src"]
 ;; langgraph brings langchain transitively (both zero-/single-dep, all
 ;; .cljc → portable to JVM / SCI / ClojureScript / GraalVM). Both live in
 ;; the kotoba-lang org of this workspace (kotoba wasm → clojurewasm →
 ;; ClojureScript → nbb runtime priority, CLAUDE.md).
 :deps {io.github.kotoba-lang/langgraph
        {:local/root "../../kotoba-lang/langgraph"}
        ;; optional: bizsupport.screening's cross-reference into
        ;; cloud-itonami-isic-8291 (com-junkawasaki/root superproject
        ;; ADR-2607115400) for operator sanctions/PEP screening before
        ;; task assignment. Only bizsupport.screening itself requires
        ;; dossier.* -- the core bizsupport.{store,policy,llm,operation}
        ;; namespaces have zero compile-time dependency on it, same
        ;; isolation shape as cloud-itonami-isic-6910's
        ;; formation.corporate-intel -> cloud-itonami-isic-8291
        ;; cross-reference. A :local/root classpath entry is not itself
        ;; I/O -- this actor still runs fully standalone/offline for any
        ;; caller that never requires bizsupport.screening.
        io.github.cloud-itonami/cloud-itonami-isic-8291
        {:local/root "../cloud-itonami-isic-8291"}}
 :aliases
 {;; offline workspace dev — pin transitive langchain to local checkout
  :dev {:override-deps
        {io.github.kotoba-lang/langchain
         {:local/root "../../kotoba-lang/langchain"}}}
  ;; clojure -M:dev:run  → drive the demo through one OperationActor
  :run {:main-opts ["-m" "bizsupport.sim"]}
  :test {:extra-paths ["test"]
         :extra-deps {io.github.cognitect-labs/test-runner
                      {:git/tag "v0.5.1" :git/sha "dfb30dd"}}
         :main-opts ["-m" "cognitect.test-runner"]
         :exec-fn cognitect.test-runner.api/test}
  ;; static analysis — clj-kondo from deps (no binary), errors fail CI.
  :lint {:replace-deps {clj-kondo/clj-kondo {:mvn/version "2024.11.14"}}
         :main-opts    ["-m" "clj-kondo.main"
                        "--lint" "src" "test"
                        "--fail-level" "error"]}}}
